name: CD

on:
    push:
        tags:
            - "v*"

    workflow_dispatch:

permissions:
    contents: write

env:
    CARGO_TERM_COLOR: always
    DATABASE_URL: postgres://mango3:mango3@127.0.0.1:5432/boards_dev

jobs:
    build_api:
        runs-on: ubuntu-24.04
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: mango3
                    POSTGRES_PASSWORD: mango3
                    POSTGRES_DB: boards_dev
                ports:
                    - 5432:5432
                options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
        steps:
            - uses: actions/checkout@v6
            - uses: ./.github/actions/setup-rust
            - run: cargo build --package boards-api --release
            - uses: actions/upload-artifact@v7
              with:
                  name: boards_api_build
                  path: ./target/release/boards-api
                  retention-days: 1
                  overwrite: true

    build_cli:
        runs-on: ubuntu-24.04
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: mango3
                    POSTGRES_PASSWORD: mango3
                    POSTGRES_DB: boards_dev
                ports:
                    - 5432:5432
                options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
        steps:
            - uses: actions/checkout@v6
            - uses: ./.github/actions/setup-rust
            - run: cargo build --package boards-cli --release
            - uses: actions/upload-artifact@v7
              with:
                  name: boards_cli_build
                  path: ./target/release/boards-cli
                  retention-days: 1
                  overwrite: true

    build_monitor:
        runs-on: ubuntu-24.04
        services:
            postgres:
                image: postgres:17-alpine
                env:
                    POSTGRES_USER: mango3
                    POSTGRES_PASSWORD: mango3
                    POSTGRES_DB: boards_dev
                ports:
                    - 5432:5432
                options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
        steps:
            - uses: actions/checkout@v6
            - uses: ./.github/actions/setup-rust
            - run: cargo build --package boards-monitor --release
            - uses: actions/upload-artifact@v7
              with:
                  name: boards_monitor_build
                  path: ./target/release/boards-monitor
                  retention-days: 1
                  overwrite: true

    dump_graphql_schema:
        needs: [build_cli]
        runs-on: ubuntu-24.04
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: mango3
                    POSTGRES_PASSWORD: mango3
                    POSTGRES_DB: boards_dev
                ports:
                    - 5432:5432
                options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5
        steps:
            - uses: actions/checkout@v6
            - uses: actions/download-artifact@v8
              with:
                  name: boards_cli_build
                  path: artifact/
            - run: |
                  chmod +x ./artifact/boards-cli
                  ./artifact/boards-cli graphql-schema | tee ./boards-${{ github.ref_name }}.graphql
            - uses: actions/upload-artifact@v7
              with:
                  name: boards_graphql_schema
                  path: ./boards-${{ github.ref_name }}.graphql
                  retention-days: 1
                  overwrite: true

    release:
        needs: [dump_graphql_schema]
        runs-on: ubuntu-24.04
        steps:
            - uses: actions/checkout@v6
            - uses: actions/download-artifact@v8
              with:
                  name: boards_graphql_schema
                  path: artifact/
            - uses: softprops/action-gh-release@v3
              with:
                  files: artifact/boards-${{ github.ref_name }}.graphql
                  generate_release_notes: true
                  prerelease: ${{ contains(github.ref_name, '-alpha') }}

    deploy:
        needs: [build_api, build_cli, build_monitor]
        runs-on: ubuntu-24.04
        steps:
            - uses: actions/checkout@v6
            - uses: actions/download-artifact@v8
              with:
                  name: boards_api_build
                  path: artifact/
            - uses: actions/download-artifact@v8
              with:
                  name: boards_cli_build
                  path: artifact/
            - uses: actions/download-artifact@v8
              with:
                  name: boards_monitor_build
                  path: artifact/
            - name: Run deploy script
              run: ${{ secrets.DEPLOY_SCRIPT }}
